"use strict";

var loginMiddleware = {};

loginMiddleware.getGithubAccessToken = function ()
{
    function __getGithubAccessToken(req, res, next)
    {
        
    }
    return __getGithubAccessToken;
}

module.exports = loginMiddleware;